
/** \brief funcion que recibe los numeros que se ingresan
 *
 * \param primer operador recibe un dato float
 * \param segund operador recibe un dato float
 * \return devuelve los numeros ingresados con float
 *
 */
float ingresarNumero(float dato);
/** \brief funcion que suma los numeros que se ingresan
 *
 * \param primer operador recibe un dato float
 * \param segundo operador recibe un dato float
 * \return devuelve los numeros ingresados con float
 *
 */
float sumaNumeros(float dato1 , float dato2);
/** \brief funcion que resta los numeros que se ingresan
 *
 * \param primer operador recibe un dato float
 * \param segundo operador recibe un dato float
 * \return devuelve los numeros ingresados con float
 *
 */
float restarNumeros(float dato1 , float dato2);
/** \brief funcion que multiplica los numeros que se ingresan
 *
 * \param primer operador recibe un dato float
 * \param segundo operador recibe un dato float
 * \return devuelve los numeros ingresados con float
 *
 */
float multiplicarNumeros( float dato1 , float dato2);
/** \brief  funcion que divide los numeros que se ingresan
 *
 * \param primer operador recibe un dato float
 * \param  segundo operador recibe un dato float
 * \return devuelve los numeros ingresados con float
 *
 */
float divisionNumeros( float operadorUno , float operadorDos);

/** \brief funcion que factoriza los numeros que se ingresan
 *
 * \param primer operador recibe un dato float
 * \param segundo operador recibe un dato float
 * \return  devuelve los numeros ingresados con float
 *
 */

float factorialPrimerOperador(float operadorUno);
